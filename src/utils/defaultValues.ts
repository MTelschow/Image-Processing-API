import { Formats } from 'resize-img';

// Just a file to centrally store constants
const DEFAULT_FILENAME: string = '';
const DEFAULT_WIDTH: number = 200;
const DEFAULT_HEIGHT: number = 200;
const DEFAULT_FORMAT: Formats = 'jpg' as Formats;

export {
  DEFAULT_FILENAME,
  DEFAULT_WIDTH,
  DEFAULT_HEIGHT,
  DEFAULT_FORMAT,
};
